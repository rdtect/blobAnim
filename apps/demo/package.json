{
  "name": "demo-hub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "node server.js",
    "dev:with-deps": "bun run --cwd ../.. build:libs && vite",
    "server:with-deps": "bun run --cwd ../.. build:libs && node server.js"
  },
  "dependencies": {
    "blob-anim": "workspace:*",
    "canvas-blob": "workspace:*",
    "pixi-blob": "workspace:*"
  },
  "devDependencies": {
    "vite": "^4.5.13"
  }
}
